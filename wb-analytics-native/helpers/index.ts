import { convertToRubleFormat } from "./converters";
import { Taxes } from "./consts";
import {
   IRowTableData,
   IRowBestCategoryTable,
   IRowBestProductTable,
   TStorageScheme,
   IResultCalculator,
   ICalculator,
} from "./types";
import { setDelay } from "./functions";

export { convertToRubleFormat, Taxes, setDelay };
export type {
   IRowTableData,
   IRowBestCategoryTable,
   IRowBestProductTable,
   IResultCalculator,
   TStorageScheme,
   ICalculator,
};
