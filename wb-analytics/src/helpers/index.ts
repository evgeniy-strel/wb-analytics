import { convertToRubleFormat } from "./converters";
import { Taxes } from "./const";
import {
   IRowTableData,
   IRowBestCategoryTable,
   IRowBestProductTable,
   TStorageScheme,
   IResultCalculator,
   ICalculator,
} from "./types";
import { setDelay, GenerateImgUrl } from "./functions";

export { convertToRubleFormat, Taxes, setDelay, GenerateImgUrl };
export type {
   IRowTableData,
   IRowBestCategoryTable,
   IRowBestProductTable,
   IResultCalculator,
   TStorageScheme,
   ICalculator,
};
